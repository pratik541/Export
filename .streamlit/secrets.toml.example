# Copy to .streamlit/secrets.toml and fill in your Supabase project's values.
# secrets.toml is gitignored — never commit real credentials.
[supabase]
url = "https://YOUR-PROJECT-ref.supabase.co"
key = "YOUR-SUPABASE-ANON-KEY"
