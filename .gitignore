__pycache__/
*.pyc
.pytest_cache/
*.xlsx
.ipynb_checkpoints/

# Local-only planning/design docs -- kept out of the repo by request.
docs/superpowers/
.superpowers/
.streamlit/secrets.toml

# Real sample shipment files (bank account, GST/PAN/IEC numbers, real
# customer name) -- local-only, never committed. *.xlsx above already
# covers two of these; this covers the whole folder including the PDF.
Data/
